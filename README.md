# IoT_Project

# Nedded packages:
- bluepy (to detect BLE devices)

Install with:
'pip install bluepy'
or (Anaconda)
''

- gtts (Google's Text to Speech API)

Install with:
'pip install gTTS'
or (Anaconda)
'conda install -c conda-forge gtts'
